#include "push_swap.h"

int	main(int argc, char *argv[])
{
	int		i;
	char	*input;

	if (argc < 2)
		exit_error("Run ./push_swap 5 2 3 1")
	i = 0;
	while (argv[++i])
		input = ft_strjoin()
	printf("number of input: %d\n", argc);
	printf("input: %s\n", argv[1]);
	return (0);
}